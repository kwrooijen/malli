(ns borkdude.dynaload
  "This namespace acts as a Mock for Dynaload. Currently dynamic
  invocation isn't supported for CLR. Dynaload + Sci need to be ported
  to CLR first.")

(defn dynaload [_ _ _])
